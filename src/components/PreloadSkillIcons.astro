---
import { skillCategories } from '../data/skills';
---

{skillCategories.map(category => (
  category.skills.map(skill => (
    <link rel="prefetch" as="image" href={`/icons/${skill.icon}.svg`} type="image/svg+xml" />
  ))
))}
